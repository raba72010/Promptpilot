# Anthropic API key — required for AI-enhanced prompt generation
# Get yours at https://console.anthropic.com/
# Without this, PromptPilot falls back to the built-in deterministic engine
ANTHROPIC_API_KEY=sk-ant-...
